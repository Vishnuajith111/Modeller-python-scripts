# Modeller-python-scripts
Modeller scripts for finding alignment between the query sequence and template sequence with structure
& modelling the query sequence using template as its reference structure
